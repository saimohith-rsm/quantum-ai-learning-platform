# ==========================================
# QuantumAI Backend Environment Configuration
# ==========================================

# Server Host & Port
PORT=8000
HOST=127.0.0.1

# MongoDB Connection (Local or MongoDB Atlas)
MONGODB_URI=mongodb+srv://rvnsm9_db_user:7tBTriERlaLMY8tX@cluster0.x0wahq2.mongodb.net/?appName=Cluster0
MONGODB_DB_NAME=quantum_learning
# For MongoDB Atlas in Cloud: mongodb+srv://username:password@cluster.mongodb.net/quantum_learning

# Authentication Secret (Change to a strong random 32+ character string in production)
JWT_SECRET=quantum-ai-sih-2026-human-x-secret-change-in-production

# CORS Allowed Origins (Comma-separated list)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# Optional AI Engine API Keys (Platform operates 100% offline with local verified quantum KB if unset)
GEMINI_API_KEY=
OPENAI_API_KEY=
DEFAULT_AI_PROVIDER=gemini
